kind: Secret
metadata:
  name: parqueadero-secrets
  labels:
    app: parqueadero
stringData:
  db_password: "CAMBIAR_POR_PASSWORD_SEGURA"
  database_url: "postgresql://parqueadero:CAMBIAR_POR_PASSWORD_SEGURA@parqueadero-db:5432/parqueadero"
  jwt_secret: "CAMBIAR_POR_CLAVE_DE_32_CARACTERES_MINIMO"
  plate_encryption_key: "CAMBIAR_POR_CLAVE_HEX_DE_64_CARACTERES_EXACTOS"
  sync_api_key: "CAMBIAR_POR_UUID_O_CLAVE_DE_16_CARACTERES_MINIMO"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: parqueadero-uploads
  labels:
    app: parqueadero
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "5Gi"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: parqueadero-backups
  labels:
    app: parqueadero
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "5Gi"
---
apiVersion: v1
kind: Pod
metadata:
  name: parqueadero-app
  labels:
    app: parqueadero
    tier: application
spec:
  containers:
    - name: backend
      image: localhost/parqueadero-backend:latest
      ports:
        - containerPort: 3000
      env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: parqueadero-secrets
              key: database_url
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: parqueadero-secrets
              key: db_password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: parqueadero-secrets
              key: jwt_secret
        - name: JWT_EXPIRATION_MINUTES
          value: "30"
        - name: PLATE_ENCRYPTION_KEY
          valueFrom:
            secretKeyRef:
              name: parqueadero-secrets
              key: plate_encryption_key
        - name: SYNC_API_KEY
          valueFrom:
            secretKeyRef:
              name: parqueadero-secrets
              key: sync_api_key
        - name: PORT
          value: "3000"
        - name: NODE_ENV
          value: "production"
        - name: SERVE_STATIC
          value: "false"
      volumeMounts:
        - name: uploads
          mountPath: /app/uploads
        - name: backups
          mountPath: /app/backups
    - name: frontend
      image: localhost/parqueadero-frontend:latest
      ports:
        - containerPort: 443
          hostPort: 3001
      volumeMounts:
        - name: tls-certs
          mountPath: /etc/nginx/ssl
          readOnly: true
  volumes:
    - name: uploads
      persistentVolumeClaim:
        claimName: parqueadero-uploads
    - name: backups
      persistentVolumeClaim:
        claimName: parqueadero-backups
    - name: tls-certs
      hostPath:
        path: /etc/parqueadero/ssl
        type: Directory
